# This file was auto-generated by Chef for <%= node[:fqdn] %>
# DO NOT edit by hand
<% @servers.each do |server| -%>
-
  url: <%= server[:url] %>
  <%= (server[:username]) ? "username: #{server[:username]}" : "" %>
  <%= (server[:password]) ? "password: #{server[:password]}" : "" %>
  <% if server[:ignore_jobs] && server[:ignore_jobs].class == "Array" %>
  ignore_jobs:
    <% server[:ignore_jobs].each do |ignore| %>
    - <%= ignore %>
    <% end %>
  <% end %>
  <% if server[:jobs] && server[:jobs].class =="Array" -%>
  jobs:
    <% server[:jobs].each do |job| %>
    - <%= job %>
    <% end %>
  <% end %>
<% end %>
